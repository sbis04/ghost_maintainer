name: ghost_maintainer_mcp
description: Ghost Maintainer - AI-powered MCP server for open-source issue management via Notion
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  dart_mcp: ^0.5.0
  http: ^1.3.0
  args: ^2.6.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.1.0
